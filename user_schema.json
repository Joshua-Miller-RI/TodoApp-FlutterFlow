{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "User",
  "description": "Schema for user documents in the 'users' collection in Firestore",
  "type": "object",
  "properties": {
    "email": {
      "description": "The email address of the user",
      "type": "string",
      "format": "email"
    },
    "display_name": {
      "description": "The display name of the user",
      "type": "string"
    },
    "photo_url": {
      "description": "The URL of the user's profile photo",
      "type": "string",
      "format": "uri"
    },
    "uid": {
      "description": "The unique identifier of the user, matching their Firebase Auth UID",
      "type": "string"
    },
    "created_time": {
      "description": "The time when the user account was created",
      "type": "string",
      "format": "date-time"
    },
    "phone_number": {
      "description": "The user's phone number",
      "type": "string"
    },
    "birthday": {
      "description": "The user's birthdate",
      "type": "string",
      "format": "date-time"
    },
    "Hometown": {
      "description": "The user's hometown",
      "type": "string"
    }
  },
  "required": ["email", "display_name", "uid", "created_time"]
}
